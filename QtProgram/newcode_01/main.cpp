/*有 n 个学生站成一排，每个学生有一个能力值，牛牛想从这 n 个学生中按照
 * 顺序选取 k 名学生，要求相邻两个学生的位置编号的差不超过 d，使得这 k
 *  个学生的能力值的乘积最大，你能返回最大的乘积吗？
*/
#include <iostream>

using namespace std;

int main()
{
    int n = 0;
    while(scanf("%d", &n) != EOF)
    {
        int [] = new int a[n];
        for(int i=0; i<n; i++)
        {
            scanf("%d", &a[i]);
        }

    }
    return 0;
}

